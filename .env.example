# Database Configuration (Local Development)
DB_CONNECTION_STRING=Host=localhost;Database=hhrr_management;Username=postgres;Password=YourPassword

# Google Gemini AI Configuration (REQUIRED)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Configuration (API Only)
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Notes:
# - For Docker deployment, only GEMINI_API_KEY is required in .env
# - Database connection is auto-configured in docker-compose.yml
# - For Gmail SMTP, use an App Password, not your regular password
# - Generate App Password at: https://myaccount.google.com/apppasswords
